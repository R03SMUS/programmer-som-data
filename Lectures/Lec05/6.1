6.1

In our humbleopnion the reuslt is as to be expected. since let x = 77 is out of scope in the addtwo function call.

The result of the last one, is a partially applied function, and thus the return is not an integer but a function that takes 1 (int) argument and adds 2. 